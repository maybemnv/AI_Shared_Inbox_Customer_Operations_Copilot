# Local fixture defaults. Copy to .env only when configuring a local run.
DEMO_MODE=fixture
API_HOST=127.0.0.1
API_PORT=8103
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Supabase values are intentionally blank. Add secrets after the code is reviewed.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8103
